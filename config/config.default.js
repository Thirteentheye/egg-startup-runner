'use strict';

/**
 * egg-startup-runner default config
 * @member Config#startupRunner
 * @property {String Array} paths - paths of folders where lies script flies, subfolders will not be processed
 */
exports.startupRunner = {
  paths: [ 'boot' ],
};
